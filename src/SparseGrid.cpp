#include "SparseGrid.h"

using namespace xform;

void SparseGrid::construct(const ImageType_1 &im_in){
  // TODO(yichang): construct from one-D image
}

void SparseGrid::construct(const XImage &im_in){
  // TODO(yichang): construct from three-dim (RGB) image
}

void SparseGrid::resample(const ImageType_1& im_in, ImageType_1* im_out) const{
  // TODO(yichang): resample the (3 dim) grid
}
void SparseGrid::resample(const XImage& im_in, ImageType_1* im_out) const{
  // TODO(yichang): resample the (5 dim) grid
}
